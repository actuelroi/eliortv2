import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Eliort votre partenaire depuis toujours",
  description: " Eliort propose son expertise en conseil, technologie, comptabilité et analyse de données pour favoriser la croissance des entreprises.",
  icons: {
    icon: [
        {
            url: "/logo/logo.svg", // /public path
            href: "/logo/logo.svg", // /public path
        },
    ],
},
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
